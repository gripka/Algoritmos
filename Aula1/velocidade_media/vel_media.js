let distancia = parseInt(prompt("Digite a distância que deseja correr: "));
let tempo = parseInt(prompt("Digite a duração da corrida em horas: "));

let velocidadeMedia = distancia / tempo;

console.log("A velocidade média necessária é: " + velocidadeMedia + " km/h");
